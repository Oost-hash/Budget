# Development Dockerfile for Frontend
FROM node:22-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including dev dependencies for Vite)
RUN npm install --verbose

# Copy TypeScript and Vite configs
COPY tsconfig*.json ./
COPY vite.config.ts ./

# Copy index.html (Vite entry point)
COPY index.html ./

# Source code will be mounted via volume for hot reload
# No COPY of src/ needed here

# Expose Vite dev server port
EXPOSE 5173

# Development command with hot reload
CMD ["npm", "run", "dev"]